---
name: ansible test
on:
  pull_request:
  push:
  schedule:
    - cron: "45 5 * * 2"

jobs:
  test:
    name: ansible test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        ansible:
          - latest ansible
          - pinned ansible
        include:
          - ansible: latest ansible
            pip3deps: ansible lxml cryptography passlib bcrypt==4.1.2
          - ansible: pinned ansible
            pip3deps: ansible==11.13.0 ansible-core==2.18.17 lxml cryptography passlib bcrypt==4.0.1
    steps:
      - uses: actions/checkout@v4
      - name: set up python
        uses: actions/setup-python@v2
        with:
          python-version: 3.13.5  # w/ 3.9 we can not install ansible >= 13
      - name: install requirements
        run: |
          python -m pip install --upgrade pip
          pip install ${{ matrix.pip3deps }}
          sudo apt install make
      - name: debug versions
        run: |
          echo "which python ..."
          which python
          echo "python -VV ..."
          python -VV
          echo "pip --version ..."
          pip --version
          echo "pip list ..."
          pip list
          echo "ansible --version ..."
          ansible --version
          echo "ansible-config dump --only-changed -t all ..."
          ansible-config dump --only-changed -t all
      - name: run ansible test
        working-directory: ./test
        run: |
          make
          ansible-playbook -e ansible_python_interpreter=$(which python) -v test.yml
